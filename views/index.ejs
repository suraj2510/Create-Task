<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title> 
     <script src="https://cdn.tailwindcss.com"></script>
     <link rel="stylesheet" href="/stylesheet/style.css">

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
          <div class="form p-8  ">
           <form action="/create" method="post">
            <input class="block w-full rounded-md outline-none p-2 text-black" type="text" placeholder="title goes here..." name="title">
            <textarea class="block resize-none w-full mt-2 rounded-md outline-none p-2 text-black" placeholder="write your task details here..." name="details"></textarea>
            <button class="submit mt-4 p-3 bg-blue-900 rounded-lg text-white">Create task</button>
           
            
           </form>
          </div>
          <div class="task p-8 flex gap-3 flex-wrap">
          <% if(files.length>0){ %>
            <% files.forEach(function(val){  %>
                <div class="task w-[200px] bg-zinc-800 rounded-md p-2 ">
                    <h1 class="text-white "><%=val%></h1>
                     <a class="text-blue-600 inline-block mt-2" href="/file/<%=val%>">Read more</a>
                  </div>
         <%   }) %>
            <% }else {%>
               <h1 class="text-white">No task yet...</h1>  

        <% }%>

          </div>
           
    </div>
</body>
</html>